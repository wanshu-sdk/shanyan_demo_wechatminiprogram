.container {
  min-height: 100vh;
  background: #f5f5f5;
  height: 100vh;
  display: flex;
  flex-direction: column;
}

/* ========== 导航栏 ========== */

.navbar {
  width: 100%;
  background: transparent;
  flex-shrink: 0;
}

.navbar-fixed {
  position: fixed;
  left: 0;
  right: 0;
  z-index: 100;
  background: #f5f5f5;
}

.navbar-content {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 88rpx;
  padding: 0 24rpx;
}

.navbar-back {
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
}

.back-icon {
  font-size: 60rpx;
  color: #333;
  line-height: 1;
}

.navbar-title {
  flex: 1;
  text-align: center;
  font-size: 34rpx;
  font-weight: 600;
  color: #333;
}

.navbar-placeholder {
  width: 60rpx;
}

.navbar-spacer {
  width: 100%;
  flex-shrink: 0;
}

/* ========== 顶部环境信息 ========== */

.header {
  text-align: center;
  padding: 20rpx 30rpx;
  flex-shrink: 0;
}

.subtitle {
  font-size: 24rpx;
  color: #999;
  display: block;
}

.env-row {
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 16rpx;
  margin-top: 8rpx;
}

.env-badge {
  font-size: 22rpx;
  color: #2b7de0;
  background: rgba(232, 240, 251, 0.85);
  border-radius: 6rpx;
  padding: 4rpx 12rpx;
}

/* ========== 步骤按钮 ========== */

.btn-group {
  padding: 0 30rpx;
  flex-shrink: 0;
}

.step-btn {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: rgba(43, 125, 224, 0.9);
  color: #fff;
  padding: 24rpx 30rpx;
  border-radius: 12rpx;
  margin-bottom: 16rpx;
  transition: all 0.2s;
}

.step-btn.disabled {
  background: #ccc;
  opacity: 0.5;
}

.step-btn.done {
  background: rgba(76, 175, 80, 0.9);
}

/* 可选步骤：浅色背景 + 虚线边框，视觉上区分必填步骤 */
.step-btn.optional {
  background: rgba(255, 255, 255, 0.9);
  color: #2b7de0;
  border: 2rpx dashed #2b7de0;
}

.step-btn.optional.done {
  background: rgba(76, 175, 80, 0.15);
  color: #2e7d32;
  border: 2rpx dashed #4caf50;
}

.step-btn.optional .btn-status {
  color: inherit;
}

/* 已完成且不可点击时，保持禁用样式 */
.step-btn.done.disabled {
  background: #ccc;
  opacity: 0.5;
}

.btn-text {
  font-size: 30rpx;
  font-weight: 500;
}

.btn-status {
  font-size: 24rpx;
  color: rgba(255, 255, 255, 0.8);
}

/* ========== 日志区域 ========== */

.log-container {
  flex: 1;
  min-height: 0;
  margin: 20rpx 30rpx;
  margin-bottom: 30rpx;
  background: rgba(30, 30, 30, 0.9);
  border-radius: 12rpx;
  overflow: hidden;
  display: flex;
  flex-direction: column;
}

.log-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15rpx 20rpx;
  background: rgba(51, 51, 51, 0.9);
  flex-shrink: 0;
}

.log-title {
  font-size: 26rpx;
  color: #fff;
  font-weight: bold;
}

.log-clear {
  font-size: 22rpx;
  color: #4caf50;
}

.log-scroll {
  flex: 1;
  min-height: 0;
}

.log-item {
  display: flex;
  padding: 8rpx 20rpx;
  border-bottom: 1rpx solid #2a2a2a;
}

.log-item.error {
  background: #2a1a1a;
}

.log-time {
  font-size: 20rpx;
  color: #888;
  margin-right: 15rpx;
  flex-shrink: 0;
}

.log-message {
  font-size: 24rpx;
  color: #e0e0e0;
  flex: 1;
  word-break: break-all;
}

.log-empty {
  padding: 40rpx 20rpx;
  text-align: center;
  color: #666;
  font-size: 24rpx;
}
